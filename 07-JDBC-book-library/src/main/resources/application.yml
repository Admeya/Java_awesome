spring:
  liquibase:
    enabled: true
    change-log: classpath:/liquibase/changelog.xml
  datasource:
    url: jdbc:h2:mem:admeyadb;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  h2:
    console:
      enabled: true
